@model IEnumerable<UL.Aria.Web.Common.Models.Shared.TaxonomyMenuItem>
@if (Model !=null && Model.Any())
{
    <ul class="page-actions">
        @foreach (dynamic item in Model)
        {
			@Helpers.ActionLink(item)
        }
    </ul>
}